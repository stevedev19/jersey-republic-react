# Copy to `.env` for local development (`.env` is gitignored).

# CRA dev server port — keep separate from the API (e.g. Express on 3003).
PORT=3000

# Backend API origin (no trailing slash). Must match your Node/Express server.
REACT_APP_API_URL=http://localhost:3003
